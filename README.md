# renovate-config

Renovate の設定済みプリセット.

`renovate.json` の `extends` に設定して使う.

```json
{
  "extends": [
    "github>hiroxto/renovate-config"
  ]
}
```

設定済みの物は以下の通り.

- pin
- lockFileMaintenance
- bundler
- docker-compose
- dockerfile
- github-actions
- gomod
- npm
