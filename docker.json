{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "description": "Docker関連の設定。`docker-compose`と`dockerfile`のアップデートを有効化するが，自動マージは全てオフに設定されている。",
    "docker-compose": {
        "enabled": true,
        "automerge": false
    },
    "dockerfile": {
        "enabled": true,
        "automerge": false
    }
}
